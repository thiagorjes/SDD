> **Claude Code**: para cada `Pergunta interativa` descrita no skill, use a ferramenta `AskUserQuestion` com os parâmetros indicados (header, multiSelect e opções). Para campos marcados como `→ texto livre`, use uma única opção "Digitar..." para que o campo "Outro" do `AskUserQuestion` seja o caminho natural.

Argumentos: $ARGUMENTS

@.agents/skills/guidelines.md
