> **Claude Code**: para cada `Pergunta interativa` descrita no skill, use a ferramenta `AskUserQuestion` com o `header` indicado, `multiSelect: false`, e as opções listadas. Se a opção for `→ Texto livre`, use uma única opção "Digitar..." para que o campo "Outro" do `AskUserQuestion` seja o caminho natural.

Argumentos: $ARGUMENTS

@.agents/skills/checklist.md
